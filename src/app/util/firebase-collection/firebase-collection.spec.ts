import { FirebaseCollection } from './firebase-collection';

describe('FirebaseCollection', () => {
  it('should create an instance', () => {
    expect(new FirebaseCollection()).toBeTruthy();
  });
});
